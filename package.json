{
  "name": "escapement",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Event-sourced scheduler for autonomous code agents",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --workspace-concurrency=1 --if-present test",
    "esc": "node apps/cli/src/esc.ts",
    "contract:emit": "pnpm --filter @escapement/store contract:emit",
    "db:plan": "pnpm --filter @escapement/store db:plan",
    "db:init": "pnpm --filter @escapement/store db:init",
    "db:bootstrap": "pnpm --filter @escapement/store db:bootstrap",
    "db:migrate": "pnpm --filter @escapement/store db:migrate",
    "db:verify": "pnpm --filter @escapement/store db:verify"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  }
}
